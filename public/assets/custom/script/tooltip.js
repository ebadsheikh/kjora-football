function openPopover(event, popoverID) {
    let element = event.target;
    while (element.nodeName !== "IMG") {
        element = element.parentNode;
    }
    var popper = Popper.createPopper(element, document.getElementById(popoverID), {
        placement: 'right'
    });
    document.getElementById(popoverID).classList.toggle("hidden");
}



function openPopover(event, accountpopoverID) {
    let element = event.target;
    while (element.nodeName !== "IMG") {
        element = element.parentNode;
    }
    var popper = Popper.createPopper(element, document.getElementById(accountpopoverID), {
        placement: 'right'
    });
    document.getElementById(accountpopoverID).classList.toggle("hidden");
}



function openPopover(event, verifypopoverID) {
    let element = event.target;
    while (element.nodeName !== "IMG") {
        element = element.parentNode;
    }
    var popper = Popper.createPopper(element, document.getElementById(verifypopoverID), {
        placement: 'right'
    });
    document.getElementById(verifypopoverID).classList.toggle("hidden");
}



function openPopover(event, playerpopoverID) {
    let element = event.target;
    while (element.nodeName !== "IMG") {
        element = element.parentNode;
    }
    var popper = Popper.createPopper(element, document.getElementById(playerpopoverID), {
        placement: 'right'
    });
    document.getElementById(playerpopoverID).classList.toggle("hidden");
}


function openPopover(event, invitepopoverID) {
    let element = event.target;
    while (element.nodeName !== "IMG") {
        element = element.parentNode;
    }
    var popper = Popper.createPopper(element, document.getElementById(invitepopoverID), {
        placement: 'right'
    });
    document.getElementById(invitepopoverID).classList.toggle("hidden");
}


function openPopover(event, stadiumpopoverID) {
    let element = event.target;
    while (element.nodeName !== "IMG") {
        element = element.parentNode;
    }
    var popper = Popper.createPopper(element, document.getElementById(stadiumpopoverID), {
        placement: 'right'
    });
    document.getElementById(stadiumpopoverID).classList.toggle("hidden");
}


function openPopover(event, upgradepopoverID) {
    let element = event.target;
    while (element.nodeName !== "IMG") {
        element = element.parentNode;
    }
    var popper = Popper.createPopper(element, document.getElementById(upgradepopoverID), {
        placement: 'right'
    });
    document.getElementById(upgradepopoverID).classList.toggle("hidden");
}


function openPopover(event, ratingpopoverID) {
    let element = event.target;
    while (element.nodeName !== "IMG") {
        element = element.parentNode;
    }
    var popper = Popper.createPopper(element, document.getElementById(ratingpopoverID), {
        placement: 'right'
    });
    document.getElementById(ratingpopoverID).classList.toggle("hidden");
}


function openPopover(event, reportpopoverID) {
    let element = event.target;
    while (element.nodeName !== "IMG") {
        element = element.parentNode;
    }
    var popper = Popper.createPopper(element, document.getElementById(reportpopoverID), {
        placement: 'right'
    });
    document.getElementById(reportpopoverID).classList.toggle("hidden");
}


function openPopover(event, chatpopoverID) {
    let element = event.target;
    while (element.nodeName !== "IMG") {
        element = element.parentNode;
    }
    var popper = Popper.createPopper(element, document.getElementById(chatpopoverID), {
        placement: 'right'
    });
    document.getElementById(chatpopoverID).classList.toggle("hidden");
}


function openPopover(event, helppopoverID) {
    let element = event.target;
    while (element.nodeName !== "IMG") {
        element = element.parentNode;
    }
    var popper = Popper.createPopper(element, document.getElementById(helppopoverID), {
        placement: 'right'
    });
    document.getElementById(helppopoverID).classList.toggle("hidden");
}


function openPopover(event, adpopoverID) {
    let element = event.target;
    while (element.nodeName !== "IMG") {
        element = element.parentNode;
    }
    var popper = Popper.createPopper(element, document.getElementById(adpopoverID), {
        placement: 'right'
    });
    document.getElementById(adpopoverID).classList.toggle("hidden");
}


function openPopover(event, paymentpopoverID) {
    let element = event.target;
    while (element.nodeName !== "IMG") {
        element = element.parentNode;
    }
    var popper = Popper.createPopper(element, document.getElementById(paymentpopoverID), {
        placement: 'right'
    });
    document.getElementById(paymentpopoverID).classList.toggle("hidden");
}


function openPopover(event, securitypopoverID) {
    let element = event.target;
    while (element.nodeName !== "IMG") {
        element = element.parentNode;
    }
    var popper = Popper.createPopper(element, document.getElementById(securitypopoverID), {
        placement: 'right'
    });
    document.getElementById(securitypopoverID).classList.toggle("hidden");
}


function openPopover(event, apppopoverID) {
    let element = event.target;
    while (element.nodeName !== "IMG") {
        element = element.parentNode;
    }
    var popper = Popper.createPopper(element, document.getElementById(apppopoverID), {
        placement: 'right'
    });
    document.getElementById(apppopoverID).classList.toggle("hidden");
}


function openPopover(event, notifypopoverID) {
    let element = event.target;
    while (element.nodeName !== "IMG") {
        element = element.parentNode;
    }
    var popper = Popper.createPopper(element, document.getElementById(notifypopoverID), {
        placement: 'right'
    });
    document.getElementById(notifypopoverID).classList.toggle("hidden");
}


function openPopover(event, settingpopoverID) {
    let element = event.target;
    while (element.nodeName !== "IMG") {
        element = element.parentNode;
    }
    var popper = Popper.createPopper(element, document.getElementById(settingpopoverID), {
        placement: 'right'
    });
    document.getElementById(settingpopoverID).classList.toggle("hidden");
}